package main {

    public class GameConst
    {
        static const startMoney = 1000;           // сколько монет выдается в начале игры
        static const hideTimeout = 1000;          // время скрытия полоски жизни (1000 - 1с)
        static const dropRadius = 200;            // разброс при вбрасывании
        static const minSpeed = 1;                // раброс начальной скорости движущегося объекта
        static const maxSpeed = 10;               // раброс начальной скорости движущегося объекта

    }
}
